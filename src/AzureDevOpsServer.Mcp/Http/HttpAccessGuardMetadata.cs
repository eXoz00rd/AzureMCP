namespace AzureDevOpsServer.Mcp.Http;

// Marks the endpoints HttpAccessMiddleware protects, so the guard follows routing's choice instead of matching the path itself.
public sealed class HttpAccessGuardMetadata
{
}
