apiVersion: v1
kind: Service
metadata:
  name: {{ include "azuremcp.fullname" . }}
  labels:
    {{- include "azuremcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "azuremcp.selectorLabels" . | nindent 4 }}
